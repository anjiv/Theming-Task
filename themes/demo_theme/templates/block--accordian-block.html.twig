{{ attach_library('demo_theme/block-styling') }}
<div{{ attributes.setAttribute('id', 'accordion') }}>
  {{ title_prefix }}
  {% if label %}
    <h2{{ title_attributes }}>{{ label }}</h2>
  {% endif %}
  {{ title_suffix }}
  {% block content %}
    {{ content }}
  {% endblock %}
</div>
